[package]
name = "canonical_lean"
version.workspace = true
edition.workspace = true

[dependencies]
canonical-compat = { path = "../canonical-compat" }
canonical-core = { path = "../canonical-core" }
canonical-heuristics = { path = "../canonical-heuristics" }
once_cell = "1.21.1"
tokio = { version = "1.44.2", features = ["full"] }

[features]
default = []
cuda = ["canonical-heuristics/cuda"]

[lib]
crate-type = ["cdylib"]
